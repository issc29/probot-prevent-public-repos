{
  "name": "prevent-public-repos",
  "version": "2.0.0",
  "description": "Probot which prevents the creation of repositories within an org which are public.",
  "author": "Isaac Cohen <issc29@github.com>",
  "license": "ISC",
  "repository": "https://github.com/issc29/prevent-public-repos.git",
  "scripts": {
    "lint": "npx standard",
    "start": "probot run ./app.js",
    "test": "jest && npm run lint"
  },
  "dependencies": {
    "probot": "^12.2.2"
  },
  "devDependencies": {
    "jest": "^27.5",
    "smee-client": "^1.1.0",
    "standard": "^12.0.1"
  },
  "engines": {
    "node": "14.x",
    "npm": "^5.3.0"
  },
  "standard": {
    "env": [
      "jest"
    ]
  },
  "jest": {
    "verbose": true,
    "testURL": "http://localhost/"
  }
}
